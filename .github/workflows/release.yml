name: Release

on:
  workflow_dispatch:
    inputs:
      bump_type:
        description: 'Version bump type'
        required: true
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major
      publish_pypi:
        description: 'Publish package to PyPI (requires trusted publishing setup)'
        required: true
        default: true
        type: boolean

jobs:
  release:
    name: Release & Publish
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write  # Required for OIDC PyPI trusted publishing
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Required for git-cliff to read commit history

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          python -m pip install build

      - name: Configure Git
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"

      - name: Get current version
        id: current_version
        run: |
          VERSION=$(grep -m 1 "^version" pyproject.toml | cut -d'"' -f2)
          echo "OLD_VERSION=$VERSION" >> $GITHUB_ENV

      - name: Bump version
        run: |
          python scripts/bump_version.py ${{ github.event.inputs.bump_type }}
          VERSION=$(grep -m 1 "^version" pyproject.toml | cut -d'"' -f2)
          echo "NEW_VERSION=$VERSION" >> $GITHUB_ENV

      - name: Generate Changelog
        uses: orhun/git-cliff-action@v4
        id: git-cliff
        with:
          config: cliff.toml
          args: --tag v${{ env.NEW_VERSION }}
        env:
          OUTPUT: CHANGELOG.md
          GITHUB_REPO: ${{ github.repository }}

      - name: Commit and Push release changes
        run: |
          git add pyproject.toml src/disentangled_flash/__init__.py CITATION.cff README.md CHANGELOG.md
          git commit -m "chore(release): prepare version v${{ env.NEW_VERSION }}"
          git tag -a "v${{ env.NEW_VERSION }}" -m "Version ${{ env.NEW_VERSION }}"
          git push origin main
          git push origin "v${{ env.NEW_VERSION }}"

      - name: Build distribution package
        run: python -m build

      - name: Publish to PyPI
        if: ${{ github.event.inputs.publish_pypi == true }}
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          skip-existing: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ env.NEW_VERSION }}
          name: v${{ env.NEW_VERSION }}
          body: ${{ steps.git-cliff.outputs.content }}
          files: |
            dist/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
